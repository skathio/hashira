# Negative-test fixture (phase 5 iteration 5.2, CHECK B).
#
# A deliberately introduced `pull_request_target` trigger in a workflow's
# `on:` block — the canonical privilege-escalation footgun B8 prohibits
# outright. `pull_request_target` runs in the BASE repo's context (base-repo
# secrets, a token scoped to the base repo) even for a fork-originated PR;
# combined with this fixture's `id-token: write` grant, this is exactly the
# escalation B8 exists to prevent. This fixture exists ONLY to prove
# tests/shape-lints/gate-lints.py's CHECK B fails closed on this exact
# defect; it is not a real workflow and is never wired into any real CI.

name: fixture-pull-request-target-workflow
on:
  pull_request_target:
  push:
    branches: [main]

permissions: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - name: Build
        run: echo "build"
